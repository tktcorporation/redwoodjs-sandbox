version: "3.7"
services:
  app:
    command: /bin/bash -c "dum i && dum rw dev"
    build: .
    working_dir: /app
    volumes:
      - ./redwoodblog:/app
    ports:
      - "8910:8910"
      - "8911:8911"
      - "18911:18911"
      - "5555:5555"
